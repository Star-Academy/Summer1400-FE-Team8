import { PersianDataPipe } from './persian-data.pipe';

describe('PersianDataPipe', () => {
  it('create an instance', () => {
    const pipe = new PersianDataPipe();
    expect(pipe).toBeTruthy();
  });
});
